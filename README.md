# 能流智枢 FlowTwin

> 油电一体化补能协同决策台 · 面向能链命题的可交互 Demo

能流智枢 FlowTwin 是一个面向油电一体化补能场景的双端决策 Demo：司机端根据出行约束规划经补能站的真实路线，运营端根据站点负载模拟生成分流策略，并通过执行闭环与验证页展示业务价值。

司机端使用真实高德地图、驾车路线和补能站 POI；站点负载、等待、价格、优惠和 ROI 使用固定种子的演示仿真数据，并在界面中明确标注。

## 本地运行

推荐使用项目自带 Node 服务。Node 20+ 无需安装额外依赖：

1. 将 `config.example.js` 复制为 `config.local.js`，填入高德 JS Key、安全密钥和 Web 服务 Key；也可以使用环境变量。
2. 启动服务：

```powershell
npm start
```

3. 打开 `http://127.0.0.1:4182/`。

服务端负责经站路线代理，Web 服务 Key 不会返回给浏览器。直接双击 HTML 或使用 Python 静态服务器只能看到固定降级场景，不能完成真实经站路线规划。

也可以用环境变量启动：

```powershell
$env:AMAP_JS_KEY="你的 Web 端 JS API Key"
$env:AMAP_SECURITY_JS_CODE="你的安全密钥"
$env:AMAP_WEB_SERVICE_KEY="你的 Web 服务 Key"
$env:PORT="4182"
npm start
```

## 部署

使用 VPS 部署时运行 `npm start`，再由 Nginx/Caddy 反向代理到 `PORT`。生产环境不要上传 `config.local.js`，改用服务器环境变量；不要把 `AMAP_WEB_SERVICE_KEY` 放进前端或公开仓库。高德 JS Key 建议配置域名白名单和调用配额。

如果只需要展示固定降级场景，可以部署 `index.html`、`app.js` 和 `config.example.js` 的静态副本；真实地图和真实经站路线需要 Node 服务提供 `/runtime-config.js` 与 `/api/route`。

## 数据边界

- 真实：地图底图、路线、POI 名称、坐标、地址。
- 演示仿真：空闲桩、未来负载、P50/P90 等待、实时价格、优惠、分流量、ROI。
- 无地图凭证或接口失败时自动切换到固定场景，核心演示不会空白。

## 演示顺序

1. 出行页：先展示能链北京总部到大兴机场的真实道路、真实补能 POI 和“最可靠”方案。
2. 点击“最快到达 / 最低成本”：观察顶部途经站点、路线和到达/超时状态同步变化。
3. 切换“纯电 / 燃油”：展示站点类型和经站路线重新计算。
4. 进入“运营”：点击提交执行审批，等待四步闭环完成，再点击“查看验证结果”。
5. 验证页：说明当前等待、价格、优惠和 ROI 是固定种子演示数据，真实 POI 与道路来自高德。
