# 能流智枢 FlowTwin V2

> AI 油电一体化补能协同决策 Demo

FlowTwin 面向能链命题，展示从车主自然语言需求到真实地图路线、补能站预测、多目标推荐、运营定价分流和实验验证的完整链路。

## 核心能力

- 豆包 AI 将中文需求解析为目的地、时间、SOC、油电类型、绕行上限、优先级和服务偏好。
- 高德地图提供真实底图、沿线补能 POI、地理编码和经站驾车路线。
- 后端生成未来 0–30 分钟、每 5 分钟一档的站点占用率与 P50/P90 等待预测。
- 车主端比较最快、最可靠和最低成本三种路线，并解释时间、等待、绕行和费用权衡。
- 运营端根据优惠金额和目标用户动态计算分流量、等待、优惠成本、增量毛利和 ROI。
- 验证页使用固定随机种子运行 30 个合成节点、1,000 次行程，对比四类策略；结果由算法实际计算。
- 执行闭环支持可选飞书机器人 Webhook；未配置时明确显示本地演示模式。

## 架构

```text
浏览器
  ├─ 高德 JS API：地图和 POI
  └─ Node.js 后端
      ├─ /api/plan              豆包 AI + 高德地理编码
      ├─ /api/route             高德经站路线代理
      ├─ /api/forecast          未来 30 分钟预测
      ├─ /api/operator/simulate 定价与分流仿真
      ├─ /api/validate          1,000 次策略实验
      └─ /api/execution         飞书执行适配器
```

模型只负责理解意图和生成解释，不直接伪造地图路线或经营指标。路线由高德计算，预测、策略和验证由后端算法计算。

## 本地运行

要求 Node.js 20+，无第三方运行时依赖。

1. 将 `config.example.js` 复制为 `config.local.js`，或使用环境变量配置服务。
2. 启动：

```powershell
npm start
```

3. 打开 <http://127.0.0.1:4182/>。

### 环境变量

```env
AMAP_JS_KEY=高德Web端JS_API_Key
AMAP_SECURITY_JS_CODE=高德安全密钥
AMAP_WEB_SERVICE_KEY=高德Web服务Key
AI_BASE_URL=https://ark.cn-beijing.volces.com/api/coding/v3
AI_API_KEY=模型服务Key
AI_MODEL=doubao-seed-2.0-lite
FEISHU_WEBHOOK_URL=可选的飞书机器人Webhook
PORT=4182
```

所有服务端密钥只从环境变量或被 Git 忽略的 `config.local.js` 读取，不会返回浏览器，也不会写入日志。

## 数据边界

- 真实数据：高德地图底图、POI 名称与坐标、地理编码、道路和经站路线。
- 演示仿真：站点实时占用率、到达率、服务率、价格、等待预测、优惠、订单和 ROI。
- 实验数据：30 个合成补能节点、1,000 次固定种子行程，用于可复现的方案比较。
- 企业数据：入围后可将仿真字段替换为能链提供的脱敏交易和站点流量数据。

## 演示流程

1. 输入：“周五18:30从能链总部去大兴机场，电量22%，不能迟到，最多绕行5公里，最好附近能吃饭”。
2. 点击 AI 按钮，观察结构化约束、真实路线和三种补能方案自动更新。
3. 查看推荐站点未来 30 分钟 P50/P90 预测和到站服务闭环。
4. 进入“运营”，调整优惠与目标用户，重新计算供需响应并提交执行。
5. 进入“验证”，查看四种策略在 1,000 次合成行程中的实际计算结果。

## 检查

```powershell
npm run check
npm test
```

## VPS 部署

运行 `npm start`，再用 Nginx 或 Caddy 反向代理到 `PORT`。生产环境通过服务端环境变量注入密钥，不要上传 `config.local.js`。高德 JS Key 应设置域名白名单和调用配额。

## License

项目代码由 `1Beyond1` 版权所有，采用 MIT License。高德地图、第三方 API、模型服务和其他外部资源不包含在本许可证授权范围内。
