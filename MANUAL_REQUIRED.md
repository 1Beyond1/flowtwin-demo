# 现场/评委前需要手动完成的事项

这份清单只列必须由项目负责人在私密环境完成的动作。代码收口本身不需要把任何 Key 写进仓库。

## 现在不需要手动做

- 不需要下载大型视觉模型；
- 不需要启动 `cv-service` 才能演示视觉合成页；
- 不需要部署 VPS；
- 不需要把 `config.local.js`、`.env`、缓存、日志或旧压缩包复制进 Git；
- 不需要为了版本检查给 GitHub 配置 Token，公开仓库可走匿名接口，但可能受 GitHub 限流影响。

## 评委前需要手动确认

1. 在私密配置中填写高德 JS Key、Web Service Key 和安全密钥，确认域名白名单；
2. 按实际主/备 AI 与 STT 服务填写配置，不要把供应商名称或 Key 写入 README；
3. 如果要展示飞书 AI，确认服务器配置的飞书应用权限、Base/表格字段和文档查看权限；
4. 启动当前工作区服务，打开本地地址，按 [`docs/测试与回归报告.md`](docs/测试与回归报告.md) 的 B01～B14 逐项走一遍；
5. 确认录屏用的是本地当前版本，不是旧 VPS 版本；
6. 录屏前清除浏览器中可能出现的个人目的地、真实车牌、Key 和调试面板。

## 如果现场网络或外部服务失败

- 高德失败：不要伪造真实路线；使用已缓存且明确标注的演示结果，或改用短途预置案例；
- AI 失败：使用本地规则降级，说明“AI 解析不可用，路线仍由规则计算”；
- 飞书失败：展示本地分流仿真，说明飞书 AI 是可选解释层；
- 版本检查失败：显示“暂时无法确认”，不要把失败当成“已是最新”；
- CV 服务未配置：展示内置合成画面，并保留“合成演示/不执行真实扣款”说明。

## 不要手动做的危险操作

- 不要把密钥粘贴到聊天、公开 issue、README 或 Git commit；
- 不要用开发机私密配置覆盖服务器私密配置；
- 不要在未确认范围时修改飞书文档公开权限或进行远程写入；
- 不要把仿真结果改成企业真实数据口径。
