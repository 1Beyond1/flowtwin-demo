# Optional only; the Node demo and synthetic CV mode do not require these.
# PaddleOCR 3.x is the preferred local CPU engine. The adapter also keeps a
# compatibility path for a prepared PaddleOCR 2.x enterprise environment.
numpy>=1.26,<3
opencv-python-headless>=4.10,<5
pillow>=10,<12
# PaddlePaddle 3.3.x currently has a known CPU PIR/oneDNN conversion crash.
# Pin the tested CPU-compatible pair instead of allowing an unsafe resolver
# upgrade during deployment.
paddlepaddle==3.2.0
paddleocr==3.3.3
