# FlowTwin 测试与回归报告

> 本报告保留历史故障与修复过程，并追加截至 2026-08-23 的源码、自动化检查与本地浏览器复测；浏览器端口和外部服务结果只对注明的那次运行负责，不能把旧服务器页面当成本地回归。

## 最新增量复测（2026-08-22）

本节是对后续版本的追加记录，不改写下方历史故障与修复过程。当前分支为 `codex/dual-mode-ai-core`，路线核心基线包含 `ca9f6ba perf: verify long trips with ordered waypoints`，评委侧 AI 体验入口最新提交为 `c4904a4 feat: surface reviewer AI follow-up control`。

- `npm run check`：通过；
- `npm test`：156/156 通过，失败 0；
- `git diff --check`：通过；
- 默认“能链北京总部 → 上海东方明珠广播电视塔”使用真实高德路线与 POI 完成规划，页面生成 **2 条去重后的可行方案**；该次稳妥方案包含 2 次补能并通过逐段能源账本校验。站点、等待和费用会随地图结果及演示压力快照变化，因此这里不把单次站名和数值写成固定基准；
- 多站方案优先以一条高德有序多途经点路线核验全程道路，再按途经点拆分账本里程；接口失败或超过 16 个途经点时保留逐段路线回退；
- 评委决策链能如实显示“规则已覆盖、未调用 AI”“高德路线 + 本地缓存命中”“企业需求先验匹配”和安全规划结果；明确输入不会为了展示而强行花 Token；
- “体验多轮 AI 补充”只把复合补充示例写入输入框，用户再次主动提交后才会按需调用模型。复合服务请求进入 AI、结构化 action 经过本地证据和白名单校验的路径已有自动化测试；本轮没有为了回归消耗一次真实模型调用；
- 1280×720 实际页面检查中，“多轮 AI 示例”已放在决策链标题栏首屏可见位置；点击后输入胶囊保持展开并显示完整示例，提交按钮变为“提交补充行程”，页面仍保持“规则校验已完成”，证明填入动作没有自动发起模型请求；
- 本轮没有执行 OCR、飞书远程写入、真实预约或支付，不能由这次路线检查推导这些外部能力当时在线可用。
- 运营页本地试算回归中，负场景 ROI 保持 `-0.91x` 并触发“不建议执行”，没有被改写成 0 或正收益；沙盘完成后流程状态从“等待仿真结果”正确切换为“等待 AI 解读”，且未自动触发飞书写入。
- 验证页固定种子结果显示排队与准时指标改善、但场景 ROI 仅 `0.05x` 时，页面明确给出“服务指标改善，但场景经济门槛未通过”“本次参数组合不建议执行”，避免只突出正向 KPI 或把合成实验包装成上线结论。
- 视觉页在本机以 PP-OCRv4 mobile 检测/识别权重、本地目录强制模式和 CPU 推理完成内置样例实测：健康检查显示 `localModelDirsReady: true`，页面返回 `inferenceStatus: executed`、车牌格式校验通过、模型分数约 99.9%，本次处理约 1.60 秒；车辆/车位检测和支付仍明确为未执行。该结果只证明当前清晰样例的本地链路，不外推为复杂场景准确率。
- 长途路线性能复测发现，第二目标的站点预测会把第一轮完整预测序列回传，形成不必要的大请求并存在超过 `/api/forecast` 128 KB 请求边界的风险。改为复用有界站点/端口标量后，在同一环境将电量从 24% 调整到 25% 以确保完整方案缓存未命中，默认北京至上海路线从约 28.8 秒降至约 4.8 秒并生成可行方案；计算期间依次显示真实阶段，不再像页面卡死。该对照使用已温热的服务端地图缓存，不代表冷启动或生产 SLA。
- 完整方案增加一份 5 分钟、单条、条件严格匹配的浏览器快照；同条件刷新实测约 1.05 秒恢复，并显示“完整补能方案缓存复用”。改变电量、动力、余量、路线或五分钟压力快照仍会重算。
- 多目标最终核验会按各路线策略自身的路线进度重新对齐站点 ETA，并在结束后恢复推荐路线的详情预测快照。浏览器复测同时发现摘要曾读取首站 P90 5.6 分钟、卡片读取全程 P90 54 分钟；现已统一为全程多次补能汇总 P90，当前同一方案两处均显示 54 分钟。调整电量再次强制缓存未命中的浏览器复测约 5.1 秒完成，路线与站点详情均正常且无控制台错误。
- 规划完成回复与方案区使用同一套去重口径：当“最快”和“最稳妥”落到同一条道路/补能序列时，回复会显示“2 条去重后的可行路线，覆盖 3 个优化目标”，不再一处称 3 条、方案区实际只显示 2 条。
- 缓存恢复方案可正常启动模拟驾驶；每个节点必须先完成自动预约并进入站内流程，“跳过本次充能演示”才会启用，行驶和预约阶段不能提前绕过。两次跳过后，“进入运营复盘”按钮按预期出现，点击后模拟结束且运营页成为当前标签。跳过节点单独计数，不会被统计为本地 OCR 或演示扣款成功；预置样例托底也与本地推理成功分开显示。过程中发现一个旧版 Lucide 图标名警告，已将不存在的 `chart-no-axes` 替换为当前版本可用的 `bar-chart-3`。
- 直达路线回归时发现，决策链虽已正确说明“未触发 OCR”，完成回顾卡仍固定显示“本地 PaddleOCR：已执行”。完成卡已改为按本轮实际集合动态生成：直达只显示无需补能与站内链路未触发；长途分别显示实际 OCR 次数、预置托底次数、完整站内回放、电子收据和跳过节点，固定场景也不再标成高德真实路线。
- 燃油与阶段门槛复核发现两处边界：燃油行驶阶段仍误提示“预约将在下一站触发”，且进入 `payment/leave` 后仍可点击跳过，可能让同一节点同时计为已扣款和已跳过。现按路线类型显示直达、燃油或充电预约文案，并把可跳过阶段收紧到识别、排队和服务；电子收据生成后只能正常确认离场，不再重复归类。

本节结论优先于下方 2026-08-12/13 的旧版本状态；旧记录保留用于说明真实出现过的问题和修复过程。

## 最新终验（2026-08-23）

本节覆盖当前工作树 `558cf92 docs: record browser award-path verification`，不改写下方历史记录：

- 主服务 `127.0.0.1:4187` 返回 `ok: true`；路线缓存统计已有 38 次命中、6 条完整方案缓存条目。
- 本地 OCR `127.0.0.1:5099` 返回 `status: ready`；服务可连接、运行时可用、本地模型目录就绪，CPU 推理完成内置样例并返回 `inferenceStatus: executed`。
- 浏览器真实走通默认“能链北京总部 → 上海东方明珠广播电视塔”路线：高德真实路线与 POI 更新，决策链显示路线缓存和企业先验输入；明确目的地请求显示“规则解析完成”，本次未调用真实模型。
- 进入运营页并点击“计算策略方案”后，页面显示预计分流 2 人、排队由 14.8 人降至 14.4 人，场景 ROI 为 `-0.91x`，并明确提示不建议发券；负 ROI 没有被改写成 0 或成功。
- 浏览器终验没有发现运行时异常，也没有点击会写入飞书的“开始智能分析”；本节不能据此证明远程飞书写入或真实 AI 供应商额度可用。
- 本轮代码与文档已保存 Git 回档点 `checkpoint/award-browser-verify-20260823`；三个交接文档仍未跟踪，没有纳入提交。

## 1. 自动化检查

执行目录：仓库根目录（`FlowTwin-Demo`）

```powershell
npm run check
npm test
git diff --check
# 使用系统或工作区提供的 Python
python -m compileall -q cv-service
python -m unittest discover -s cv-service -p "test_*.py"
```

当前收口基线（2026-08-13）：

- `npm run check`：通过；
- `npm test`：138/138 通过，失败 0；
- `git diff --check`：通过；
- `cv-service/.venv/Scripts/python.exe -m compileall -q cv-service`：通过；
- Python CV 适配器单元测试：7/7 通过；
- 当前检出分支：`codex/dual-mode-ai-core`；本次回归基于本地最新工作树，上一提交基线为 `7e0f96d docs: note cleaned training environment`；本地 `origin/main` 为 `4e820d8`，当前分支尚未推送；
- `1328f9a`、`d2d9d19`、`04c7766` 属于历史功能存档点，不作为当前版本基线；
- 两份交接文档仍保持未跟踪，没有进入该存档点。

当前版本已经包含混动能源分支隔离、企业需求先验、动态置信度、视觉安全降级、版本检查、限流和模拟驾驶演示收口等历史修复；本次清理了已不再需要的企业训练虚拟环境，保留模型导出、评估结果和 Demo runtime 产物。训练环境清理不改变 Node Demo 运行链路。

本轮本地 UI 回归还覆盖了：新规划默认展开完整方案比较面板；桌面端方案卡片不再嵌套滚动；长模拟场景改由外层面板统一滚动；滚动条降为低对比度细条；模拟驾驶默认开启“路线自动、站内手动”和 3× 行驶；非油服务推荐保持边框提示；工具箱支持跳过当前补能节点；模拟驾驶期间隐藏顶部重复的车辆到站标识。

## 2. 自动化覆盖范围

- 高德主备 Key 轮换、文件缓存、并发去重和旧缓存降级；
- AI 解析失败降级、主备模型、当前时间/当前电量不由模型臆造；
- 新行程与多轮补充 action；
- 全国地点解析和地理歧义提示；
- 排队预测、P50/P90 同源关系、天气因子、端口级离散事件模拟；
- 动态预测置信度和依据字段；
- 长途多站、6 站正常上限、12 站自适应预算、重复站点去重和搜索不完整状态；
- 最快/稳妥/最低成本与 Pareto/重复方案合并；
- 运营仿真、平台可执行站点过滤、飞书本地降级；
- CV 合成结果、上传 fallback、PNG/JPEG/WebP 文件头校验、上游残缺成功结果校验；
- API 限流、可信代理 IP、执行幂等和版本检查降级。

## 3. 浏览器回归清单

每次改动 `app.js`、`index.html` 或服务端接口后，至少走以下路径：

| 编号 | 场景 | 预期 |
| --- | --- | --- |
| B01 | 首次打开 | 不展示旧路线或机场目的地，显示待规划状态 |
| B02 | 默认东方明珠示例 | 一次点击完成解析和路线规划，目的地在上海，不出现邵阳/机场回退 |
| B03 | 100% 纯电短途 | 可直达时不强制充电 |
| B04 | 5% 纯电同一目的地 | 第一站按安全可达性重新选择，不沿用满电站点 |
| B05 | 燃油/混动切换 | 站点类型、单位和算法分支同步变化 |
| B06 | 布达拉宫/跨省长途 | 需要时出现多站；若搜索预算不完整，显示明确提示 |
| B07 | 三目标相同 | 合并重复路线，卡片数量和“方案”文案一致 |
| B08 | 多轮“中途想吃饭” | 追加服务，不把原行程全部推倒；无可行候选时如实说明 |
| B09 | 预测依据 | 能展开来源、方法、到站时间、P50/P90 和动态置信度原因 |
| B10 | 运营分析 | 仿真和飞书 AI 分两步显示，飞书未配置不影响本地仿真 |
| B11 | 设置页 | 评审/用户模式、语音自动规划开关、版本检查均可用 |
| B12 | 视觉页 | 内置样例与上传图片/短视频在本地 CV 服务和模型可用时走 PaddleOCR；服务不可用或未识别时明确标“未执行/未识别”，不把 fallback 当识别成功 |
| B13 | 小屏/宽页面 | 右侧面板不发生横向裁切；隐藏后可再次展开 |
| B14 | 语音录音 | 点击后有录音状态动画，未录音时静止；结束后按设置进入规划或输入框 |

### 历史浏览器结果（2026-08-10，本地 4197 端口）

以下结果只说明 2026-08-10 那次运行，不代表当前 4182 端口或线上版本：

- **通过**：B01 首屏待规划；B02 东方明珠默认示例；B03 100% 纯电短途直达且不强制补能；B04 5% 纯电前往布达拉宫进入低电量安全规划，首段约 12.8 km，生成 11 次自适应补能，最终到达余量保持在安全下限以上；B05 燃油切换后站点变为油站、单位变为 L，混动切换后出现油电对比并按选中的能源路径生成路线；B07 三目标相同的路线合并为一张“最佳方案”；B08 多轮输入“中途想吃点东西”被识别为补充需求，并在当前约束下无安全服务候选时如实提示未加入；B09 动态置信度、预测依据和 AI/规则解析依据可展开；B10 本地运营分流仿真生成了券档位、承接站、引导量、排队变化和场景 ROI；B11 设置页、版本检查、评审/用户模式；B12 本地 OCR 视觉分析；B13 1280×720 页面无横向溢出。
- **本轮补充通过**：输入“从能链北京总部去南京，优先准时”并选择“南京南站”后，输入框被替换为“从能链北京总部去南京南站，优先准时”，不会再出现“南京南站南京”；路线仍能正常规划。预测依据折叠面板、视觉合成页、设置页版本检查和用户模式切换也在 4197 端口重新走通。
- **本轮补充通过**：评审模式 AI/规则对比展开后，未填写的到达余量显示为“—”，不再把 JavaScript 的 `null` 误显示成 `0%`；路线卡片仍显示“未设到达余量要求”。
- **部分验证**：B06 已用布达拉宫极端长途验证 11 次补能和沿线兜底候选，尚未在本轮重新覆盖“搜索预算耗尽”的提示分支；B12 已分别验证内置样例/上传图片真实 OCR 路径和无服务 fallback：未配置视觉推理服务时明确显示“未执行/未识别”，不输出伪造识别结果；B10 没有点击会写入飞书的“开始智能分析”；B14 已检查未录音时按钮静止、点击后的 UI 状态路径，真实麦克风录音仍取决于浏览器权限，未在本轮授权验证。
- **服务候选边界**：候选加入失败时显示具体的绕行、时间或补能约束原因，不伪造“服务已加入”；混动切换后的沿线兜底候选也不再复用旧能源分支状态。

历史浏览器实测使用了本机私密配置中的高德/AI 服务，但没有读取、输出或写入任何 Key；当时版本检查结果为“当前为尚未发布的开发版本”，对应的是当时本地 commit 尚未推送的状态，不应继续作为当前版本结论。

### 修复前浏览器复测基线（2026-08-12，本地 4182 端口）

本次使用真实本地浏览器从首屏开始操作，服务进程保持可访问；`/api/health` 返回 `ok: true`，但以下结果只代表本次 4182 端口运行。测试过程中没有读取或输出任何密钥，也没有执行飞书远程写入或上传私人文件。

| 场景 | 结果 | 证据与复现摘要 |
| --- | --- | --- |
| 首次模式选择 | PASS | 首次打开出现“评委模式/用户模式”选择框；选择不被记住。 |
| 默认东方明珠规划 | **FAIL（本次运行）** | 点击默认“从能链北京总部前往上海东方明珠广播电视塔，优先准时”后，地图目的地正确且真实高德路线返回，但页面显示“0 条可行 · 2 条备用”“多站补能候选未能完成计算”，两张卡均为“无法安全到站”。本次不能把它写成默认主线已通过；需要继续查候选搜索预算、上游返回或缓存状态。 |
| 100%/短途直达 | PASS | 改为“从能链北京总部去天安门广场，优先准时”后，页面显示“全优方案”“直达·无需补能”，到达余量 19.3%，明确说明取消不必要的补能停靠。 |
| 重复路线合并 | PARTIAL | 同一条天安门短途路线最终只显示一张“最佳方案”，没有三张重复卡；但路线区域标题仍显示“当前方案不可执行”，与卡片“最佳方案/无需补能”相矛盾，这是当前明确的 UI 状态文案问题。 |
| 视觉页入口与安全降级 | PASS | 视觉页可进入，能看到内置样例和上传入口；点击“识别内置样例”后如实显示“未执行/未识别”，不伪造车牌号、不执行支付。 |
| 本地 OCR 实际执行 | **未验证（当前环境未执行）** | 同一次操作明确提示“当前运行环境未配置 OpenCV/Paddle 推理服务”。健康检查中的 `cv.configured: true` 只表示可选 CV 配置存在，不等于推理服务和模型已就绪；本次没有得到 OCR 车牌结果。 |
| 设置与版本检查 | PASS | 设置页显示 `v1.1.0`、短 commit `4e820d8`；点击“检查更新”显示“已是最新”。 |
| 用户模式 | PASS | 切换后运营、验证、视觉等评委信息被收起/隐藏，保留用户侧路线、补能和等待信息。上述路线标题矛盾在用户模式仍可见。 |
| 运营、飞书、验证、语音 | 未验证（本次未重跑） | 本次没有为了扩大高德/外部服务消耗而重复点击这些流程；继续沿用历史记录：本地运营仿真已通过，飞书远程写入和真实麦克风权限仍不能由本次检查证明。 |

#### 修复前复测发现的三个问题（已在上方复测）

1. **默认东方明珠长途结果不稳定/曾失败**：修复前出现过 0 条可行方案；修复后改为服务端真实路线优先、有限重试与明确失败边界，见上方“默认东方明珠冷启动”。
2. **可执行路线的状态标题错误**：已改为优先使用可行目标别名，天安门直达回归的标题和卡片均为“最佳方案”。
3. **CV 健康状态语义过宽**：已拆分“配置存在”“服务可连接”“运行时可用”“模型已加载”“可推理”五个状态；当前本机服务未启动时，页面会明确显示未连接。

本次浏览器实测使用端口 `4182`；页面和健康检查在测试期间保持可访问，没有出现服务进程崩溃。`4197` 仅是历史浏览器测试端口，不能当作当前默认端口。以下问题已经在后面的修复后复测中重新验证，不能继续当作当前版本结论。

### 修复后浏览器复测（2026-08-12，本地 4182 端口）

修复提交：`04c7766 fix: stabilize route fallback and vision readiness`。本次从重新加载开始，先按实际入口选择“评委模式”，再提交默认东方明珠示例，避免模式选择弹窗遮挡提交控件造成误判。

| 场景 | 结果 | 修复与证据 |
| --- | --- | --- |
| 默认东方明珠冷启动 | PASS | 完整流程得到 `2 条可行方案`，两张方案均为真实路线复核后的多站补能路线；当前实测显示 3 次补能、首选路线绕行 1.2 km、到达余量 2%，地图状态为“高德地图已连接 · 真实路线与 POI 已更新”。没有出现“0 条可行/无法安全到站”。 |
| 基础路线请求稳定性 | PASS（有限恢复） | 基础路线不再把三条浏览器 SDK 请求一次性 `Promise.all` 打爆：改为服务端高德路线适配器优先，服务端已有磁盘缓存；服务端失败才按错峰、最多一次 SDK 重试回退。后端规划器本身的 `PLANNER_UNAVAILABLE` 也只重试一次。所有恢复路径仍要求真实路线返回，失败时继续报失败，不使用固定演示折线。 |
| 重复路线标题 | PASS | 天安门直达回归显示 `heading "最佳方案"`，卡片同样显示“最佳方案/无需补能”；标题不再显示“当前方案不可执行”。分组代表优先选择已通过可行性校验的目标别名，避免卡片和区域标题读取不同状态。 |
| 视觉健康状态与真实 OCR | PASS | `/api/cv/health` 区分 `serviceReachable`、`runtimeAvailable`、`modelLoaded`、`inferenceReady` 与 `status`；本轮启动本地 PaddleOCR 服务后返回 `status: ready`。通过主服务 `/api/cv/analyze` 将内置样例按上传图片路径送入本地模型，返回 `inferenceStatus: executed`、符合格式校验的识别文本和 PaddleOCR 模型分数；未执行车辆检测、车位检测或支付。 |
| 自动化检查 | PASS | 修复后重新执行 `npm run check`、`npm test`、`git diff --check`：135/135 测试通过，语法与差异检查通过。 |

这次复测中，曾有一次刷新后未先处理模式选择弹窗就尝试提交；该次不作为路线失败证据。正确操作顺序是：刷新 → 选择评委模式或用户模式 → 输入/确认行程 → 提交规划。

本轮视觉实测使用临时本地端口 `5100`，默认部署端口仍为 `5099`；模型权重来自本机已有的 PaddleOCR 缓存，没有上传第三方或提交到仓库。若服务未启动，页面仍应按安全降级显示“未执行/未识别”，不能把样例或 fallback 当成 OCR 成功。高德上游配额、域名白名单或真实网络中断仍可能导致路线失败；当前修复只减少客户端初始化和短暂请求抖动，不会把真实上游失败改写成成功。

## 4. 发现问题时的记录格式

```text
时间：
浏览器与窗口尺寸：
输入与前置状态：
操作步骤：
实际结果：
预期结果：
控制台/Network 摘要（不得包含 Key）：
截图路径：
是否阻塞录屏：
```

## 5. 仍不能由自动化测试证明的内容

自动化测试没有证明高德配额充足、线上域名白名单正确、飞书远程写入权限正确、真实 AI/STT 供应商可用或生产并发达到某个数字。它们需要在不暴露密钥的本机/服务器环境中单独验证。真实麦克风录音也不能只靠代码测试证明，现场需要手动授予浏览器麦克风权限。
